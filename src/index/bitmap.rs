//! Bitmap index for filtering

/// Bitmap index for fast filtering
pub struct BitmapIndex {
    // TODO: Implement bitmap index
}

impl BitmapIndex {
    pub fn new() -> Self {
        Self {}
    }
}

impl Default for BitmapIndex {
    fn default() -> Self {
        Self::new()
    }
}
