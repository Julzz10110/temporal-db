//! Gossip protocol for membership

/// Gossip node for membership management
pub struct GossipNode {
    // TODO: Implement gossip protocol
}

impl GossipNode {
    pub fn new() -> Self {
        Self {}
    }
}

impl Default for GossipNode {
    fn default() -> Self {
        Self::new()
    }
}
